name: Phase 5C shared memory implementation

on:
  push:
    branches: [feat/phase-5c-module-forms-conformance]
    paths:
      - .github/workflows/phase5c-memory-implementation.yml

permissions:
  contents: write

jobs:
  implement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/phase-5c-module-forms-conformance
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Apply shared imported memory slice
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          p = Path('crates/wasm-runtime/src/lib.rs')
          s = p.read_text()

          def rep(old, new):
              global s
              if old not in s:
                  raise SystemExit('expected snippet not found:\n' + old[:300])
              s = s.replace(old, new, 1)

          table_tail = '''    fn function_index_for_instance(
              &self,
              index: u32,
              owner: &Rc<()>,
          ) -> Result<Option<u32>, TableHandleError> {
              let Some(function) = self.get(index)? else {
                  return Ok(None);
              };
              let Some(actual_owner) = function.owner.upgrade() else {
                  return Err(TableHandleError::ForeignFunctionReference { index });
              };
              if !Rc::ptr_eq(&actual_owner, owner) {
                  return Err(TableHandleError::ForeignFunctionReference { index });
              }
              Ok(Some(function.function_index))
          }
      }

      pub struct HostContext<'a> {
          memory: Option<&'a mut LinearMemory>,
          capabilities: HostCapabilities,
      }
      '''
          memory_types = '''    fn function_index_for_instance(
              &self,
              index: u32,
              owner: &Rc<()>,
          ) -> Result<Option<u32>, TableHandleError> {
              let Some(function) = self.get(index)? else {
                  return Ok(None);
              };
              let Some(actual_owner) = function.owner.upgrade() else {
                  return Err(TableHandleError::ForeignFunctionReference { index });
              };
              if !Rc::ptr_eq(&actual_owner, owner) {
                  return Err(TableHandleError::ForeignFunctionReference { index });
              }
              Ok(Some(function.function_index))
          }
      }

      #[derive(Debug, Clone, PartialEq, Eq)]
      pub enum MemoryHandleError {
          InvalidLimits { minimum: u32, maximum: u32 },
          LimitExceeded { pages: u32, limit: u32 },
          AllocationFailed { pages: u32 },
          OutOfBounds { address: u64, width: usize },
      }

      impl fmt::Display for MemoryHandleError {
          fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
              match self {
                  Self::InvalidLimits { minimum, maximum } => write!(
                      f,
                      "memory minimum {minimum} exceeds declared maximum {maximum}"
                  ),
                  Self::LimitExceeded { pages, limit } => {
                      write!(f, "memory limit {pages} pages exceeds WebAssembly limit {limit}")
                  }
                  Self::AllocationFailed { pages } => {
                      write!(f, "failed to allocate linear memory with {pages} pages")
                  }
                  Self::OutOfBounds { address, width } => write!(
                      f,
                      "memory access at byte {address} with width {width} is out of bounds"
                  ),
              }
          }
      }

      impl std::error::Error for MemoryHandleError {}

      #[derive(Clone)]
      pub struct MemoryHandle {
          memory: Rc<RefCell<LinearMemory>>,
          minimum: u32,
          maximum: Option<u32>,
      }

      impl fmt::Debug for MemoryHandle {
          fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
              f.debug_struct("MemoryHandle")
                  .field("minimum", &self.minimum)
                  .field("maximum", &self.maximum)
                  .field("size_pages", &self.size_pages())
                  .finish()
          }
      }

      impl MemoryHandle {
          pub fn new(minimum: u32, maximum: Option<u32>) -> Result<Self, MemoryHandleError> {
              if let Some(maximum) = maximum {
                  if minimum > maximum {
                      return Err(MemoryHandleError::InvalidLimits { minimum, maximum });
                  }
                  if maximum > MAX_MEMORY_PAGES {
                      return Err(MemoryHandleError::LimitExceeded {
                          pages: maximum,
                          limit: MAX_MEMORY_PAGES,
                      });
                  }
              }
              if minimum > MAX_MEMORY_PAGES {
                  return Err(MemoryHandleError::LimitExceeded {
                      pages: minimum,
                      limit: MAX_MEMORY_PAGES,
                  });
              }
              let byte_len = pages_to_bytes(minimum)
                  .ok_or(MemoryHandleError::AllocationFailed { pages: minimum })?;
              let mut bytes = Vec::new();
              bytes
                  .try_reserve_exact(byte_len)
                  .map_err(|_| MemoryHandleError::AllocationFailed { pages: minimum })?;
              bytes.resize(byte_len, 0);
              let max_pages = maximum.unwrap_or(MAX_MEMORY_PAGES);
              Ok(Self {
                  memory: Rc::new(RefCell::new(LinearMemory { bytes, max_pages })),
                  minimum,
                  maximum,
              })
          }

          pub fn minimum(&self) -> u32 {
              self.minimum
          }

          pub fn maximum(&self) -> Option<u32> {
              self.maximum
          }

          pub fn size_pages(&self) -> u32 {
              self.memory.borrow().size_pages()
          }

          pub fn read(&self, address: u32, length: usize) -> Result<Vec<u8>, MemoryHandleError> {
              let memory = self.memory.borrow();
              let range = memory
                  .checked_host_range(address, length)
                  .map_err(|error| match error {
                      HostError::MemoryOutOfBounds { address, width } => {
                          MemoryHandleError::OutOfBounds { address, width }
                      }
                      _ => unreachable!("checked_host_range only reports bounds errors"),
                  })?;
              Ok(memory.bytes[range].to_vec())
          }

          pub fn write(&self, address: u32, bytes: &[u8]) -> Result<(), MemoryHandleError> {
              let mut memory = self.memory.borrow_mut();
              let range = memory
                  .checked_host_range(address, bytes.len())
                  .map_err(|error| match error {
                      HostError::MemoryOutOfBounds { address, width } => {
                          MemoryHandleError::OutOfBounds { address, width }
                      }
                      _ => unreachable!("checked_host_range only reports bounds errors"),
                  })?;
              memory.bytes[range].copy_from_slice(bytes);
              Ok(())
          }

          pub fn grow(&self, delta_pages: u32) -> i32 {
              self.memory.borrow_mut().grow(delta_pages)
          }
      }

      enum HostMemory<'a> {
          Owned(&'a mut LinearMemory),
          Shared(MemoryHandle),
      }

      pub struct HostContext<'a> {
          memory: Option<HostMemory<'a>>,
          capabilities: HostCapabilities,
      }
      '''
          rep(table_tail, memory_types)

          old_host_methods = '''        self.memory
              .as_deref()
              .map(LinearMemory::size_pages)
              .ok_or(HostError::MemoryUnavailable)
          }

          pub fn read_memory(&self, address: u32, length: usize) -> Result<Vec<u8>, HostError> {
              if !self.capabilities.memory_read {
                  return Err(HostError::CapabilityDenied("memory.read"));
              }
              let memory = self.memory.as_deref().ok_or(HostError::MemoryUnavailable)?;
              let range = memory.checked_host_range(address, length)?;
              Ok(memory.bytes[range].to_vec())
          }

          pub fn write_memory(&mut self, address: u32, bytes: &[u8]) -> Result<(), HostError> {
              if !self.capabilities.memory_write {
                  return Err(HostError::CapabilityDenied("memory.write"));
              }
              let memory = self
                  .memory
                  .as_deref_mut()
                  .ok_or(HostError::MemoryUnavailable)?;
              let range = memory.checked_host_range(address, bytes.len())?;
              memory.bytes[range].copy_from_slice(bytes);
              Ok(())
          }
      }
      '''
          new_host_methods = '''        match self.memory.as_ref().ok_or(HostError::MemoryUnavailable)? {
              HostMemory::Owned(memory) => Ok(memory.size_pages()),
              HostMemory::Shared(memory) => Ok(memory.size_pages()),
          }
          }

          pub fn read_memory(&self, address: u32, length: usize) -> Result<Vec<u8>, HostError> {
              if !self.capabilities.memory_read {
                  return Err(HostError::CapabilityDenied("memory.read"));
              }
              match self.memory.as_ref().ok_or(HostError::MemoryUnavailable)? {
                  HostMemory::Owned(memory) => {
                      let range = memory.checked_host_range(address, length)?;
                      Ok(memory.bytes[range].to_vec())
                  }
                  HostMemory::Shared(memory) => memory.read(address, length).map_err(|error| {
                      match error {
                          MemoryHandleError::OutOfBounds { address, width } => {
                              HostError::MemoryOutOfBounds { address, width }
                          }
                          _ => HostError::MemoryUnavailable,
                      }
                  }),
              }
          }

          pub fn write_memory(&mut self, address: u32, bytes: &[u8]) -> Result<(), HostError> {
              if !self.capabilities.memory_write {
                  return Err(HostError::CapabilityDenied("memory.write"));
              }
              match self.memory.as_mut().ok_or(HostError::MemoryUnavailable)? {
                  HostMemory::Owned(memory) => {
                      let range = memory.checked_host_range(address, bytes.len())?;
                      memory.bytes[range].copy_from_slice(bytes);
                      Ok(())
                  }
                  HostMemory::Shared(memory) => memory.write(address, bytes).map_err(|error| {
                      match error {
                          MemoryHandleError::OutOfBounds { address, width } => {
                              HostError::MemoryOutOfBounds { address, width }
                          }
                          _ => HostError::MemoryUnavailable,
                      }
                  }),
              }
          }
      }
      '''
          rep(old_host_methods, new_host_methods)

          rep('''    DuplicateTable { module: String, name: String },
          UnsupportedSignature,''', '''    DuplicateTable { module: String, name: String },
          DuplicateMemory { module: String, name: String },
          UnsupportedSignature,''')
          rep('''            Self::DuplicateTable { module, name } => {
                  write!(f, "host table {module}.{name} is already registered")
              }
              Self::UnsupportedSignature''', '''            Self::DuplicateTable { module, name } => {
                  write!(f, "host table {module}.{name} is already registered")
              }
              Self::DuplicateMemory { module, name } => {
                  write!(f, "host memory {module}.{name} is already registered")
              }
              Self::UnsupportedSignature''')
          rep('''    tables: HashMap<(String, String), TableHandle>,
      }''', '''    tables: HashMap<(String, String), TableHandle>,
          memories: HashMap<(String, String), MemoryHandle>,
      }''')
          rep('''.field("table_count", &self.tables.len())
                  .finish()''', '''.field("table_count", &self.tables.len())
                  .field("memory_count", &self.memories.len())
                  .finish()''')
          rep('''        self.tables.insert(key, table);
              Ok(())
          }
      }''', '''        self.tables.insert(key, table);
              Ok(())
          }

          pub fn register_memory(
              &mut self,
              module: impl Into<String>,
              name: impl Into<String>,
              memory: MemoryHandle,
          ) -> Result<(), HostRegistryError> {
              let module = module.into();
              let name = name.into();
              let key = (module.clone(), name.clone());
              if self.memories.contains_key(&key) {
                  return Err(HostRegistryError::DuplicateMemory { module, name });
              }
              self.memories.insert(key, memory);
              Ok(())
          }
      }''')

          rep('''    UnresolvedTableImport {
              module: String,
              name: String,
          },
          HostTableLimitsMismatch {''', '''    UnresolvedTableImport {
              module: String,
              name: String,
          },
          UnresolvedMemoryImport {
              module: String,
              name: String,
          },
          HostMemoryLimitsMismatch {
              module: String,
              name: String,
              expected_minimum: u32,
              expected_maximum: Option<u32>,
              actual_minimum: u32,
              actual_maximum: Option<u32>,
          },
          HostMemoryRuntimeLimitMismatch {
              module: String,
              name: String,
              memory_limit: u32,
              runtime_limit: u32,
          },
          HostTableLimitsMismatch {''')
          rep('''            Self::UnresolvedTableImport { module, name } => {
                  write!(f, "unresolved host table import {module}.{name}")
              }
              Self::HostTableLimitsMismatch {''', '''            Self::UnresolvedTableImport { module, name } => {
                  write!(f, "unresolved host table import {module}.{name}")
              }
              Self::UnresolvedMemoryImport { module, name } => {
                  write!(f, "unresolved host memory import {module}.{name}")
              }
              Self::HostMemoryLimitsMismatch {
                  module,
                  name,
                  expected_minimum,
                  expected_maximum,
                  actual_minimum,
                  actual_maximum,
              } => write!(
                  f,
                  "host memory {module}.{name} has limits min={actual_minimum} max={actual_maximum:?}, which do not satisfy imported min={expected_minimum} max={expected_maximum:?}"
              ),
              Self::HostMemoryRuntimeLimitMismatch {
                  module,
                  name,
                  memory_limit,
                  runtime_limit,
              } => write!(
                  f,
                  "host memory {module}.{name} can reach {memory_limit} pages, exceeding runtime limit {runtime_limit}"
              ),
              Self::HostTableLimitsMismatch {''')

          rep('''    memory: Option<LinearMemory>,
          table: Option<TableHandle>,''', '''    memory: Option<LinearMemory>,
          imported_memory: Option<MemoryHandle>,
          table: Option<TableHandle>,''')
          rep('''        reject_unsupported_object_imports(&module)?;
              validate_host_bindings(&module, &hosts)?;''', '''        reject_unsupported_object_imports(&module)?;
              validate_host_bindings(&module, &hosts, limits)?;''')
          old_memory_init = '''        let memory = module
              .memories
              .first()
              .map(|memory_type| {
                  LinearMemory::new(
                      memory_type.limits.min,
                      memory_type.limits.max,
                      limits.max_memory_pages,
                  )
              })
              .transpose()?;
          let identity = Rc::new(());'''
          new_memory_init = '''        let imported_memory = instantiate_imported_memory(&module, &hosts, limits)?;
          let memory = if imported_memory.is_none() {
              module
                  .memories
                  .first()
                  .map(|memory_type| {
                      LinearMemory::new(
                          memory_type.limits.min,
                          memory_type.limits.max,
                          limits.max_memory_pages,
                      )
                  })
                  .transpose()?
          } else {
              None
          };
          let identity = Rc::new(());'''
          rep(old_memory_init, new_memory_init)
          rep('''            memory,
              table,''', '''            memory,
              imported_memory,
              table,''')

          old_data = '''    fn initialize_data_segments(&mut self) -> Result<(), RuntimeError> {
          for (segment_index, segment) in self.module.data.iter().enumerate() {
              let offset = u64::from(segment.offset as u32);
              let end = offset.checked_add(segment.bytes.len() as u64).ok_or(
                  RuntimeError::DataSegmentOutOfBounds {
                      segment: segment_index,
                      offset,
                      length: segment.bytes.len(),
                  },
              )?;
              let memory = self
                  .memory
                  .as_mut()
                  .ok_or(RuntimeError::MemoryUnavailable)?;
              if end > memory.bytes.len() as u64 {
                  return Err(RuntimeError::DataSegmentOutOfBounds {
                      segment: segment_index,
                      offset,
                      length: segment.bytes.len(),
                  });
              }
              memory.bytes[offset as usize..end as usize].copy_from_slice(&segment.bytes);
          }
          Ok(())
      }'''
          new_data = '''    fn initialize_data_segments(&mut self) -> Result<(), RuntimeError> {
          let data = self.module.data.clone();

          // Preflight all active segments before mutating a potentially host-shared memory.
          for (segment_index, segment) in data.iter().enumerate() {
              let offset = u64::from(segment.offset as u32);
              let end = offset.checked_add(segment.bytes.len() as u64).ok_or(
                  RuntimeError::DataSegmentOutOfBounds {
                      segment: segment_index,
                      offset,
                      length: segment.bytes.len(),
                  },
              )?;
              let memory_len = self.with_memory(|memory| Ok(memory.bytes.len() as u64))?;
              if end > memory_len {
                  return Err(RuntimeError::DataSegmentOutOfBounds {
                      segment: segment_index,
                      offset,
                      length: segment.bytes.len(),
                  });
              }
          }

          for segment in &data {
              let offset = u64::from(segment.offset as u32);
              self.with_memory_mut(|memory| {
                  let start = usize::try_from(offset).map_err(|_| {
                      RuntimeError::ControlInvariant("preflighted data offset no longer fits usize")
                  })?;
                  let end = start + segment.bytes.len();
                  memory.bytes[start..end].copy_from_slice(&segment.bytes);
                  Ok(())
              })?;
          }
          Ok(())
      }'''
          rep(old_data, new_data)

          old_mem_helpers = '''    fn memory_ref(&self) -> Result<&LinearMemory, RuntimeError> {
          self.memory.as_ref().ok_or(RuntimeError::MemoryUnavailable)
      }

      fn memory_mut(&mut self) -> Result<&mut LinearMemory, RuntimeError> {
          self.memory.as_mut().ok_or(RuntimeError::MemoryUnavailable)
      }
      '''
          new_mem_helpers = '''    fn with_memory<R>(
          &self,
          f: impl FnOnce(&LinearMemory) -> Result<R, RuntimeError>,
      ) -> Result<R, RuntimeError> {
          if let Some(memory) = self.memory.as_ref() {
              return f(memory);
          }
          if let Some(memory) = self.imported_memory.as_ref() {
              let memory = memory.memory.borrow();
              return f(&memory);
          }
          Err(RuntimeError::MemoryUnavailable)
      }

      fn with_memory_mut<R>(
          &mut self,
          f: impl FnOnce(&mut LinearMemory) -> Result<R, RuntimeError>,
      ) -> Result<R, RuntimeError> {
          if let Some(memory) = self.memory.as_mut() {
              return f(memory);
          }
          if let Some(memory) = self.imported_memory.as_ref() {
              let mut memory = memory.memory.borrow_mut();
              return f(&mut memory);
          }
          Err(RuntimeError::MemoryUnavailable)
      }
      '''
          rep(old_mem_helpers, new_mem_helpers)

          old_invoke_host = '''        let key = (import.module.clone(), import.name.clone());
          let (hosts, memory) = (&mut self.hosts, &mut self.memory);
          let host = hosts
              .functions
              .get_mut(&key)
              .ok_or_else(|| RuntimeError::UnresolvedImport {
                  module: import.module.clone(),
                  name: import.name.clone(),
              })?;
          let mut context = HostContext {
              memory: memory.as_mut(),
              capabilities: host.capabilities,
          };'''
          new_invoke_host = '''        let key = (import.module.clone(), import.name.clone());
          let (hosts, memory, imported_memory) =
              (&mut self.hosts, &mut self.memory, &self.imported_memory);
          let host = hosts
              .functions
              .get_mut(&key)
              .ok_or_else(|| RuntimeError::UnresolvedImport {
                  module: import.module.clone(),
                  name: import.name.clone(),
              })?;
          let context_memory = if let Some(shared) = imported_memory.as_ref() {
              Some(HostMemory::Shared(shared.clone()))
          } else {
              memory.as_mut().map(HostMemory::Owned)
          };
          let mut context = HostContext {
              memory: context_memory,
              capabilities: host.capabilities,
          };'''
          rep(old_invoke_host, new_invoke_host)

          for old, new in [
              ('self.memory_ref()?.load_i32(address, displacement)?', 'self.with_memory(|memory| memory.load_i32(address, displacement))?'),
              ('self.memory_ref()?.load_i8_s(address, displacement)?', 'self.with_memory(|memory| memory.load_i8_s(address, displacement))?'),
              ('self.memory_ref()?.load_i8_u(address, displacement)?', 'self.with_memory(|memory| memory.load_i8_u(address, displacement))?'),
              ('self.memory_ref()?.load_i16_s(address, displacement)?', 'self.with_memory(|memory| memory.load_i16_s(address, displacement))?'),
              ('self.memory_ref()?.load_i16_u(address, displacement)?', 'self.with_memory(|memory| memory.load_i16_u(address, displacement))?'),
              ('self.memory_mut()?.store_i32(address, displacement, value)?', 'self.with_memory_mut(|memory| memory.store_i32(address, displacement, value))?'),
              ('self.memory_mut()?.store_i8(address, displacement, value)?', 'self.with_memory_mut(|memory| memory.store_i8(address, displacement, value))?'),
              ('self.memory_mut()?.store_i16(address, displacement, value)?', 'self.with_memory_mut(|memory| memory.store_i16(address, displacement, value))?'),
              ('self.memory_ref()?.size_pages() as i32', 'self.with_memory(|memory| Ok(memory.size_pages()))? as i32'),
              ('self.memory_mut()?.grow(delta)', 'self.with_memory_mut(|memory| Ok(memory.grow(delta)))?'),
          ]:
              rep(old, new)

          rep('''            ImportDesc::Function(_) | ImportDesc::Global(_) | ImportDesc::Table(_) => {}''', '''            ImportDesc::Function(_)
              | ImportDesc::Global(_)
              | ImportDesc::Table(_)
              | ImportDesc::Memory(_) => {}''')
          rep('''fn validate_host_bindings(module: &Module, hosts: &HostRegistry) -> Result<(), RuntimeError> {''', '''fn validate_host_bindings(
          module: &Module,
          hosts: &HostRegistry,
          limits: RuntimeLimits,
      ) -> Result<(), RuntimeError> {''')

          table_loop_marker = '''    for import in &module.imports {
          let ImportDesc::Table(table_type) = import.desc else {
              continue;
          };
          let key = (import.module.clone(), import.name.clone());
          let table = hosts
              .tables
              .get(&key)
              .ok_or_else(|| RuntimeError::UnresolvedTableImport {
                  module: import.module.clone(),
                  name: import.name.clone(),
              })?;
          validate_table_limits(import, table_type.limits.min, table_type.limits.max, table)?;
      }

      for import in &module.imports {
          let ImportDesc::Global(global_type) = import.desc else {'''
          table_plus_memory = '''    for import in &module.imports {
          let ImportDesc::Table(table_type) = import.desc else {
              continue;
          };
          let key = (import.module.clone(), import.name.clone());
          let table = hosts
              .tables
              .get(&key)
              .ok_or_else(|| RuntimeError::UnresolvedTableImport {
                  module: import.module.clone(),
                  name: import.name.clone(),
              })?;
          validate_table_limits(import, table_type.limits.min, table_type.limits.max, table)?;
      }

      for import in &module.imports {
          let ImportDesc::Memory(memory_type) = import.desc else {
              continue;
          };
          let key = (import.module.clone(), import.name.clone());
          let memory = hosts
              .memories
              .get(&key)
              .ok_or_else(|| RuntimeError::UnresolvedMemoryImport {
                  module: import.module.clone(),
                  name: import.name.clone(),
              })?;
          validate_memory_limits(import, memory_type.limits.min, memory_type.limits.max, memory)?;
          validate_memory_runtime_limit(import, memory, limits.max_memory_pages)?;
      }

      for import in &module.imports {
          let ImportDesc::Global(global_type) = import.desc else {'''
          rep(table_loop_marker, table_plus_memory)

          insert_before_table_validation = '''fn validate_table_limits(
      import: &wasm_parser::Import,'''
          memory_validation = '''fn validate_memory_limits(
          import: &wasm_parser::Import,
          expected_minimum: u32,
          expected_maximum: Option<u32>,
          memory: &MemoryHandle,
      ) -> Result<(), RuntimeError> {
          let actual_minimum = memory.minimum();
          let actual_maximum = memory.maximum();
          let minimum_matches = actual_minimum >= expected_minimum;
          let maximum_matches = match expected_maximum {
              None => true,
              Some(expected) => matches!(actual_maximum, Some(actual) if actual <= expected),
          };
          if minimum_matches && maximum_matches {
              return Ok(());
          }
          Err(RuntimeError::HostMemoryLimitsMismatch {
              module: import.module.clone(),
              name: import.name.clone(),
              expected_minimum,
              expected_maximum,
              actual_minimum,
              actual_maximum,
          })
      }

      fn validate_memory_runtime_limit(
          import: &wasm_parser::Import,
          memory: &MemoryHandle,
          runtime_limit: u32,
      ) -> Result<(), RuntimeError> {
          let runtime_limit = runtime_limit.min(MAX_MEMORY_PAGES);
          let memory_limit = memory.maximum().unwrap_or(MAX_MEMORY_PAGES);
          if memory.size_pages() <= runtime_limit && memory_limit <= runtime_limit {
              return Ok(());
          }
          Err(RuntimeError::HostMemoryRuntimeLimitMismatch {
              module: import.module.clone(),
              name: import.name.clone(),
              memory_limit,
              runtime_limit,
          })
      }

      fn instantiate_imported_memory(
          module: &Module,
          hosts: &HostRegistry,
          limits: RuntimeLimits,
      ) -> Result<Option<MemoryHandle>, RuntimeError> {
          for import in &module.imports {
              let ImportDesc::Memory(memory_type) = import.desc else {
                  continue;
              };
              let key = (import.module.clone(), import.name.clone());
              let memory = hosts.memories.get(&key).cloned().ok_or_else(|| {
                  RuntimeError::UnresolvedMemoryImport {
                      module: import.module.clone(),
                      name: import.name.clone(),
                  }
              })?;
              validate_memory_limits(
                  import,
                  memory_type.limits.min,
                  memory_type.limits.max,
                  &memory,
              )?;
              validate_memory_runtime_limit(import, &memory, limits.max_memory_pages)?;
              return Ok(Some(memory));
          }
          Ok(None)
      }

      fn validate_table_limits(
      import: &wasm_parser::Import,'''
          rep(insert_before_table_validation, memory_validation)

          rep('''    if index != 0 || instance.memory.is_none() {''', '''    if index != 0 || (instance.memory.is_none() && instance.imported_memory.is_none()) {''')

          p.write_text(s)

          test = r'''use wasm_parser::parse_module;
      use wasm_runtime::{
          HostRegistry, HostRegistryError, Instance, MemoryHandle, MemoryHandleError, RuntimeError,
          RuntimeLimits, Value,
      };

      fn push_u32(bytes: &mut Vec<u8>, mut value: u32) {
          loop {
              let mut byte = (value & 0x7f) as u8;
              value >>= 7;
              if value != 0 { byte |= 0x80; }
              bytes.push(byte);
              if value == 0 { break; }
          }
      }

      fn push_name(bytes: &mut Vec<u8>, name: &str) {
          push_u32(bytes, name.len() as u32);
          bytes.extend_from_slice(name.as_bytes());
      }

      fn push_section(module: &mut Vec<u8>, id: u8, payload: &[u8]) {
          module.push(id);
          push_u32(module, payload.len() as u32);
          module.extend_from_slice(payload);
      }

      fn imported_memory_module() -> Vec<u8> {
          let mut module = vec![0x00, 0x61, 0x73, 0x6d, 0x01, 0x00, 0x00, 0x00];
          push_section(
              &mut module,
              1,
              &[
                  0x03,
                  0x60, 0x01, 0x7f, 0x01, 0x7f,
                  0x60, 0x02, 0x7f, 0x7f, 0x00,
                  0x60, 0x00, 0x01, 0x7f,
              ],
          );
          let mut imports = vec![0x01];
          push_name(&mut imports, "env");
          push_name(&mut imports, "mem");
          imports.extend([0x02, 0x01, 0x02, 0x04]);
          push_section(&mut module, 2, &imports);
          push_section(&mut module, 3, &[0x04, 0x00, 0x01, 0x02, 0x00]);

          let mut exports = vec![0x04];
          for (name, index) in [("load", 0u32), ("store", 1), ("size", 2), ("grow", 3)] {
              push_name(&mut exports, name);
              exports.push(0x00);
              push_u32(&mut exports, index);
          }
          push_section(&mut module, 7, &exports);

          let bodies: [&[u8]; 4] = [
              &[0x00, 0x20, 0x00, 0x28, 0x02, 0x00, 0x0b],
              &[0x00, 0x20, 0x00, 0x20, 0x01, 0x36, 0x02, 0x00, 0x0b],
              &[0x00, 0x3f, 0x00, 0x0b],
              &[0x00, 0x20, 0x00, 0x40, 0x00, 0x0b],
          ];
          let mut code = vec![bodies.len() as u8];
          for body in bodies {
              push_u32(&mut code, body.len() as u32);
              code.extend_from_slice(body);
          }
          push_section(&mut module, 10, &code);
          push_section(
              &mut module,
              11,
              &[0x01, 0x00, 0x41, 0x00, 0x0b, 0x04, b'A', b'B', b'C', b'D'],
          );
          module
      }

      fn instantiate(memory: &MemoryHandle) -> Instance {
          let module = parse_module(&imported_memory_module()).expect("parse imported-memory fixture");
          let mut hosts = HostRegistry::new();
          hosts.register_memory("env", "mem", memory.clone()).unwrap();
          Instance::with_hosts(module, hosts).expect("instantiate imported-memory fixture")
      }

      #[test]
      fn data_segment_and_wasm_memory_ops_use_shared_imported_backing() {
          let memory = MemoryHandle::new(2, Some(4)).unwrap();
          let mut vm = instantiate(&memory);
          assert_eq!(memory.read(0, 4).unwrap(), b"ABCD");
          assert_eq!(
              vm.invoke_export("load", &[Value::I32(0)]).unwrap(),
              Some(Value::I32(i32::from_le_bytes(*b"ABCD")))
          );

          memory.write(8, &5i32.to_le_bytes()).unwrap();
          assert_eq!(
              vm.invoke_export("load", &[Value::I32(8)]).unwrap(),
              Some(Value::I32(5))
          );

          assert_eq!(
              vm.invoke_export("store", &[Value::I32(12), Value::I32(77)]).unwrap(),
              None
          );
          assert_eq!(memory.read(12, 4).unwrap(), 77i32.to_le_bytes());
      }

      #[test]
      fn memory_grow_and_size_are_bidirectionally_visible() {
          let memory = MemoryHandle::new(2, Some(4)).unwrap();
          let mut vm = instantiate(&memory);
          assert_eq!(vm.invoke_export("grow", &[Value::I32(1)]).unwrap(), Some(Value::I32(2)));
          assert_eq!(memory.size_pages(), 3);
          assert_eq!(memory.grow(1), 3);
          assert_eq!(vm.invoke_export("size", &[]).unwrap(), Some(Value::I32(4)));
          assert_eq!(vm.invoke_export("grow", &[Value::I32(1)]).unwrap(), Some(Value::I32(-1)));
      }

      #[test]
      fn one_memory_handle_can_back_multiple_live_instances() {
          let memory = MemoryHandle::new(2, Some(4)).unwrap();
          let mut first = instantiate(&memory);
          let mut second = instantiate(&memory);
          first.invoke_export("store", &[Value::I32(16), Value::I32(99)]).unwrap();
          assert_eq!(
              second.invoke_export("load", &[Value::I32(16)]).unwrap(),
              Some(Value::I32(99))
          );
      }

      #[test]
      fn imported_memory_limits_follow_wasm_subtyping_and_runtime_caps() {
          for memory in [
              MemoryHandle::new(1, Some(4)).unwrap(),
              MemoryHandle::new(2, None).unwrap(),
              MemoryHandle::new(2, Some(5)).unwrap(),
          ] {
              let module = parse_module(&imported_memory_module()).unwrap();
              let mut hosts = HostRegistry::new();
              hosts.register_memory("env", "mem", memory).unwrap();
              assert!(matches!(
                  Instance::with_hosts(module, hosts),
                  Err(RuntimeError::HostMemoryLimitsMismatch { .. })
              ));
          }

          let compatible = MemoryHandle::new(3, Some(3)).unwrap();
          let mut vm = instantiate(&compatible);
          assert_eq!(vm.invoke_export("size", &[]).unwrap(), Some(Value::I32(3)));

          let module = parse_module(&imported_memory_module()).unwrap();
          let mut hosts = HostRegistry::new();
          hosts.register_memory("env", "mem", MemoryHandle::new(2, Some(4)).unwrap()).unwrap();
          let limits = RuntimeLimits { max_memory_pages: 3, ..RuntimeLimits::default() };
          assert!(matches!(
              Instance::with_config(module, hosts, limits),
              Err(RuntimeError::HostMemoryRuntimeLimitMismatch { .. })
          ));
      }

      #[test]
      fn unresolved_and_duplicate_memory_bindings_fail_closed() {
          let module = parse_module(&imported_memory_module()).unwrap();
          assert!(matches!(
              Instance::new(module),
              Err(RuntimeError::UnresolvedMemoryImport { .. })
          ));

          let memory = MemoryHandle::new(2, Some(4)).unwrap();
          let mut hosts = HostRegistry::new();
          hosts.register_memory("env", "mem", memory.clone()).unwrap();
          assert_eq!(
              hosts.register_memory("env", "mem", memory),
              Err(HostRegistryError::DuplicateMemory { module: "env".into(), name: "mem".into() })
          );
      }

      #[test]
      fn memory_handle_rejects_bad_limits_and_oob_access() {
          assert_eq!(
              MemoryHandle::new(3, Some(2)).unwrap_err(),
              MemoryHandleError::InvalidLimits { minimum: 3, maximum: 2 }
          );
          let memory = MemoryHandle::new(1, Some(1)).unwrap();
          assert!(matches!(
              memory.read(65535, 2),
              Err(MemoryHandleError::OutOfBounds { address: 65535, width: 2 })
          ));
      }
      '''
          Path('crates/wasm-runtime/tests/phase5c_imported_memory.rs').write_text(test)
          PY

          cargo fmt --all
          cargo fmt --all -- --check
          cargo clippy --workspace --all-targets -- -D warnings
          cargo test --workspace --all-targets
          cargo doc --workspace --no-deps

      - name: Commit validated slice and remove staging workflow
        shell: bash
        run: |
          rm .github/workflows/phase5c-memory-implementation.yml
          git config user.name "Lei-TzuY"
          git config user.email "52287354+Lei-TzuY@users.noreply.github.com"
          git add -- crates/wasm-runtime/src/lib.rs crates/wasm-runtime/tests/phase5c_imported_memory.rs .github/workflows/phase5c-memory-implementation.yml
          git commit -m "feat: add shared imported memory backing"
          git push origin HEAD:feat/phase-5c-module-forms-conformance
