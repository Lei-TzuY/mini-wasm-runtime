name: Conquest 262 qualification

on:
  push:
    branches:
      - conquest/relaxed-simd-f32x4-nmadd

permissions:
  contents: write

jobs:
  qualify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Apply 262 slice
        run: python3 tools/conquest_apply_262.py
      - name: Format
        run: cargo fmt --all
      - name: Format check
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Workspace tests
        run: cargo test --workspace
      - name: Docs
        run: cargo doc --workspace --no-deps
      - name: Wasmtime differential
        run: cargo test --manifest-path differential/Cargo.toml --test simd_relaxed_nmadd_f32x4
      - name: Promote clean candidate
        run: |
          rm -f tools/conquest_apply_262.py .github/workflows/conquest-262-qualification.yml
          git add -A
          test -n "$(git status --porcelain)"
          git config user.name "LeiZ"
          git config user.email "52287354+Lei-TzuY@users.noreply.github.com"
          git commit -m "feat: add relaxed SIMD f32x4 nmadd"
          git push origin HEAD:conquest/relaxed-simd-f32x4-nmadd
