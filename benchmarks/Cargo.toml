[workspace]
members = ["."]
resolver = "2"

[package]
name = "mini-wasm-benchmarks"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
wasm-parser = { path = "../crates/wasm-parser" }
wasm-runtime = { path = "../crates/wasm-runtime" }
wat = "=1.257.1"

[lints.rust]
unsafe_code = "forbid"
